<%- include('./partials/head') %>

<%- include('./partials/sidebar', { active: 'legal-pages' }) %>

<main class="flex-1">
  <div class="px-6 lg:px-10 py-8">
    <div class="flex items-start justify-between gap-6">
      <div>
        <%- include('./partials/breadcrumb', { breadcrumbs: [{label: 'Pages légales'}] }) %>
        <div class="text-2xl md:text-3xl font-semibold text-slate-900">Pages légales</div>
        <div class="mt-1 text-sm text-slate-500">Modifiez CGV, CGU, mentions légales, confidentialité…</div>
      </div>

      <a class="btn btn-secondary" href="/legal" target="_blank" rel="noreferrer">
        Voir côté site
      </a>
    </div>

    <% if (errorMessage) { %>
      <div class="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-red-900">
        <div class="font-bold">Erreur</div>
        <div class="mt-1 text-sm"><%= errorMessage %></div>
      </div>
    <% } %>

    <% if (successMessage) { %>
      <div class="mt-6 rounded-xl border border-green-200 bg-green-50 p-4 text-green-900">
        <div class="font-bold">OK</div>
        <div class="mt-1 text-sm"><%= successMessage %></div>
      </div>
    <% } %>

    <% if (!dbConnected) { %>
      <div class="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4 text-amber-900">
        <div class="font-bold">Mode démo</div>
        <div class="mt-1 text-sm">La base de données est indisponible : vous ne pouvez pas enregistrer vos modifications.</div>
      </div>
    <% } %>

    <div class="mt-8 bg-white rounded-2xl border border-slate-200 overflow-hidden">
      <div class="px-6 py-4 bg-slate-50">
        <div class="font-semibold text-slate-900">Liste</div>
        <div class="mt-1 text-xs text-slate-500"><%= pages && pages.length ? pages.length : 0 %> page(s)</div>
      </div>

      <% if (pages && pages.length) { %>
        <div class="divide-y divide-slate-100">
          <% pages.forEach((p) => { %>
            <div class="p-6 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
              <div>
                <div class="font-semibold text-slate-900"><%= p.title %></div>
                <div class="mt-1 text-xs text-slate-500">Slug : <%= p.slug %></div>
              </div>

              <div class="flex items-center gap-2">
                <a class="btn btn-primary" href="/admin/pages-legales/<%= p.slug %>">
                  Éditer
                </a>
                <a class="btn btn-secondary" href="/legal/<%= p.slug %>" target="_blank" rel="noreferrer">
                  Voir
                </a>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <div class="p-6 text-slate-500">Aucune page.</div>
      <% } %>
    </div>
  </div>
</main>

<%- include('./partials/footer') %>
